# PostgreSQL Connection String (or leave empty to use embedded fallback)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/coffeeshop?sslmode=disable

# Or specify individual parameters
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=coffeeshop

PORT=8080
